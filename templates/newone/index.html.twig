{% extends 'base.html.twig' %}

{% block title %}Newone index{% endblock %}

{% block body %}
    <h1>Newone index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Date</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for newone in newones %}
            <tr>
                <td>{{ newone.id }}</td>
                <td>{{ newone.nom }}</td>
                <td>{{ newone.date ? newone.date|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('app_newone_show', {'id': newone.id}) }}">show</a>
                    <a href="{{ path('app_newone_edit', {'id': newone.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_newone_new') }}">Create new</a>
{% endblock %}
